import { Button, ButtonRow, DangerButton, PrimaryButton, SecondaryButton } from './Button.style'

/**
 * The button component
 *
 */

export { Button, ButtonRow, DangerButton, PrimaryButton, SecondaryButton }

Button.defaultProps = {
  children: 'Button',
  large: false,
  async: false
}
